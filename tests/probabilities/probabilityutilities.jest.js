const { isConfirmatoryEvidence, calculateBayesPosterior, calculateIndependantIntersection, calculateUnion, calculateDependent } = require("../../src/probabilities/probabilityutilities.js");

test('returns true for confirmatory evidence', () => {
    fail();
});

